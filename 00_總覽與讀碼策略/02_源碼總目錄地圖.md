# 源碼總目錄地圖

## 根目錄

- `.github`：GitHub 工作流程與專案協作設定
- `assets`：靜態資源
- `build`：打包腳本
- `dist`：打包後產物
- `examples`：範例與文件展示
- `src`：核心源碼
- `test`：測試
- `types`：型別宣告

## src 目錄

- `components`：所有元件
- `directives`：自訂指令
- `locale`：國際化
- `mixins`：共用混入邏輯
- `styles`：Less 樣式系統
- `utils`：工具函式
- `index.js`：元件庫入口
